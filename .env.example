# Sacred Escrow CLI Configuration
# Copy this file to .env and fill in your values

# Required: JSON-RPC endpoint for blockchain interaction
RPC_URL=https://testnet-passet-hub-eth-rpc.polkadot.io

# Optional: Chain ID (will be auto-detected if not provided)
CHAIN_ID=420420422

# Required: Deployed SacredEscrow contract address
ESCROW_ADDRESS=0x4ed8F631E73A1e4aCf535894E11B033aba4936D5

# Private keys (without 0x prefix or with 0x prefix)
# Required for deposit/refund operations
RELAYER_PRIVATE_KEY=your_private_key_here

# Required for creating attestation signatures
ATTESTER_PRIVATE_KEY=your_attester_private_key_here

# Optional: You can also pass keys via command line flags
# --depositor-key, --relayer-key, --attester-key
